#include <stdio.h>
#include <stdlib.h>

int main(){
    
    if((-5 || 0)&&(3 >= 2)&&(1 != 0)||(3 < 0))
           printf("Verdadeiro\n");
    else   printf("Falso\n");
    system("PAUSE");
    return 0;
}

